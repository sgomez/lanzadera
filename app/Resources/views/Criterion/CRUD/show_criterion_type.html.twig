{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}
{% block field %}
    {% spaceless %}
        {% if value is empty %}
            &nbsp;
        {% else %}
            {{ ('label.'~value)|trans({}) }}
        {% endif %}
    {% endspaceless %}
{% endblock %}