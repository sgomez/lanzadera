{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}
{% block field %}
    {% spaceless %}
        {% if value is empty %}
            &nbsp;
        {% else %}
            {{ ('lanzadera.criterion.type.'~value)|trans({}) }}
        {% endif %}
    {% endspaceless %}
{% endblock %}